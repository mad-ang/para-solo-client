import AWS from 'aws-sdk';

import { v4 as uuidv4 } from 'uuid';
import { updateUserInfo } from './auth';

// 이미지 한개 업로드
export const addImage = (albumName: string, files: any, next: any): void => {};
